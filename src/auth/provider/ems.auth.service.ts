import { Injectable } from '@nestjs/common';

@Injectable()
export class EmsAuthService {
  constructor() // private readonly configService: ConfigService, // private readonly jwtService: JwtService, // private readonly prismaService: PrismaService,
  // @Inject(JWT_OPTIONS)
  // private readonly jwtOption: JwtOption,
  // private readonly authService: AuthService,
  {}

  async login() {
  
  
  }
}
